# E-University Telemetry Stack - Environment Variables Template
# Copy this file to .env and fill in secure values
# Generate secure passwords with: python3 -c "import secrets; print(secrets.token_urlsafe(24))"

# InfluxDB Configuration
INFLUXDB_USERNAME=admin
INFLUXDB_PASSWORD=<generate-secure-password>
INFLUXDB_ORG=euniv
INFLUXDB_BUCKET=network_telemetry
INFLUXDB_TOKEN=<generate-secure-token>

# Grafana Configuration
GRAFANA_USER=admin
GRAFANA_PASSWORD=<generate-secure-password>

# Device credentials are inherited from parent project's .env
# Ensure DEVICE_USERNAME, DEVICE_PASSWORD, DEVICE_ENABLE_PASSWORD are set
